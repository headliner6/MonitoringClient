<UserControl x:Class="MonitoringClient.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:MonitoringClient.View"
             xmlns:vm="clr-namespace:MonitoringClient.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:NavigationViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:LogEntryViewModel}">
            <view:LogEntryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LogMessageAddViewModel}">
            <view:LogMessageAddView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LocationViewModel}">
            <view:LocationView/>
        </DataTemplate>
    </UserControl.Resources>
    <ContentControl x:Name="Pages" Content="{Binding SelectedViewModel}"/>
</UserControl>
